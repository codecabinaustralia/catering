<%= form_for(meal_option) do |f| %>
  <% if meal_option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(meal_option.errors.count, "error") %> prohibited this meal_option from being saved:</h2>

      <ul>
      <% meal_option.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

    <div class="field">
    <%= f.label :color %>
    <%= f.text_field :color, class: "color_wheel" %>
  </div>

  <div class="field">
    <%= f.label :meal_category_id %>
    <%= f.collection_select :meal_category_id, MealCategory.all,:id,:title %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
